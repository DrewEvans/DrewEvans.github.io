{
	"name": "reactexpress",
	"version": "1.0.0",
	"description": "Boilerplate React & Express",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node server.js",
		"server": "nodemon server.js",
		"client": "npm start --prefix client",
		"dev": "concurrently \"npm run server\" \"npm run client\""
	},
	"author": "Drew Evans",
	"license": "MIT",
	"dependencies": {
		"concurrently": "^6.1.0",
		"cors": "^2.8.5",
		"express": "^4.17.1",
		"gh-pages": "^3.2.0",
		"node-sass": "^6.0.0",
		"save-dev": "0.0.1-security"
	},
	"devDependencies": {
		"nodemon": "^2.0.7"
	}
}
